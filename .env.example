# DebridPulse — environment variables
# Kopieren nach .env und anpassen.

# Zeitzone
TZ=Europe/Berlin

# Optional: public base URL of the app, so Discord can fetch uploaded avatars
# from outside via /api/avatar.
# Beispiel:
# PUBLIC_BASE_URL=https://example.com

# PostgreSQL, when used, is configured as an external server in Settings → Database.
