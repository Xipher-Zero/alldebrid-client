name: Tests

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply-v106-event-boundary-cleanup:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout work branch
        uses: actions/checkout@v5
        with:
          ref: work/v1.0.6-audit-cleanup
          fetch-depth: 0

      - name: Apply deterministic final boundary cleanup
        run: python tools/v106_event_boundary_cleanup.py

      - name: Remove helper and commit source only
        run: |
          git rm tools/v106_event_boundary_cleanup.py
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A -- ':!.github/workflows/tests.yml'
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Complete v1.0.6 service event boundary"
          git push origin HEAD:work/v1.0.6-audit-cleanup
